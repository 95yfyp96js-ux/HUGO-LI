name: web_ledger
description: 小額借款＋支票貼現：網頁版第 1 版（伺服器端計算，前端不算金額）。
publish_to: none
environment:
  sdk: ^3.5.0

dependencies:
  lending_engine:
    path: ../../packages/lending_engine
  shelf: ^1.4.0
  shelf_router: ^1.1.4
  sqlite3: ^2.4.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
