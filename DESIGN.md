# DESIGN.md

視覺契約。以下四條是結構性規則，不是風格指南。
違反任一條即為錯誤實作，不接受「這次例外比較好看」。

調色盤只准五個角色：`void`／`ash`／`ember`／`flame`／`paper`。
沒有淺色模式。這不是深色主題，這就是產品的樣子。

字體以 Noto Serif TC 為正文。上線必須自架，禁止依賴 Google Fonts CDN。
全站唯一的深色文字，只出現在 `<Paper>` 上，且為墨色。

---

## 1. 一個畫面一盎燈

光由 `<Light>` 產生。頁面不得直接使用 `<Light>`。
一盎燈只能經由下列兩者之一的唯一 `light` prop 進入畫面：

- `CeremonyScreen`
- `PageScreen`

架構上不得並存第二盎。測試應斷言同一 route 的 light instance ≤ 1。

光釘在真正發光的物件上（香爐、燭火、已安奉的燈）。
光不移動；是人走近。亮度隨儀式階段變化，不隨方案、不隨訂閱等級變化。

衰減偏快。慢衰減會把畫面變成棕霧，而不是「一盎燈照亮一圈」。

禁止偽第二光源：描邊按鈕、發光 focus ring、亮色 toast、支付成功煙火、loading 比香火更亮。
按鈕只有拉開的字距加一條細線。

訂閱／方案差異用日期與文字表達，不用更亮的火。

---

## 2. 對稱是儀式，不對稱是管理

每個 route 必須宣告自己是哪一種，不得混種。

| 模式 | 殼 | 佈局 | 光源 | 適用 |
|---|---|---|---|---|
| 儀式 | `CeremonyScreen` | 置中對稱 | 有，且唯一 | 拜拜、抽籤、點燈（火亮起來的那一段） |
| 管理 | `PageScreen` | 靠左，右邊留空 | 無，或極弱環境光且仍遵守一盎 | 首頁、探索、紀錄、香燈年約、家庭名冊、祭改、金流、物流 |

點燈的填單（燈種、姓名、生辰、地址、付款）是管理。
點燈的「火亮起來」是儀式。兩者不得同一畫面。

祭改名稱像儀式，實際是預約表單。必須走管理側：靠左、無光源強調、無戲劇性。
真正的祭改在廟裡發生，不在這個畫面裡。

---

## 3. 紙只給真的是紙的東西

`<Paper>` 只用在：

- 籤紙
- 祈願卡（拜拜的願望）
- 點燈名條
- 疏文副本
- 還願卡
- 廟方安奉確認後可保存的紙憑據

紙上的字是墨色，是全站唯一深色文字。

以下不是紙，禁止包進 `<Paper>`：

- AI 解籤／解讀
- 電子收據、訂單、物流狀態
- 訂閱方案說明、價格表
- 系統通知、錯誤訊息
- 祭改表單

材質差異同時是內容安全設計：旁人的說明，不是神明給的那張。

---

## 4. 不用裝飾圖案

導覽用字，不用宗教符號當圖示。
禁止把分屬不同宗教體系的符號混在一起「看起來很東方」。
特別禁止把具有實際宗教意義、又極易被誤讀的符號當作 UI chrome。

金只用箔的份量，不用漸層金屬框。
朱（印泥紅）若使用，只作為「廟方已完成」的一次性印記，不得成為品牌主色。

動效預算：火微晃、煙上升、紙展開。
禁止：搖手機爆香、功德數字跳動、燈海粒子、排行榜。
