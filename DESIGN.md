# DESIGN — 視覺方向

> 目標感覺：**「一座夜裡的宮廟，只有香爐和燭火發光，其他都在暗處。」**

這份文件是視覺決策的依據。任何新畫面、新元件都必須通過這裡的四條規則；
如果一個設計要違反規則，必須先在這裡寫下理由，而不是默默做掉。

---

## 規則一：一個畫面只有一個光源

- 光由 `<Light>` 元件產生，而且**頁面不能直接使用它**——只能透過 `CeremonyScreen`／`PageScreen`
  各自唯一的 `light` prop 取得一盞光。這讓規則由架構保證，而不是靠自律。
- 光的位置固定在該畫面「真正發光的那個東西」上（香爐、燭火、燈），
  亮度隨儀式進行改變。**光不移動——是我們越走越近。**
- 光的衰減刻意做得比一般 UI 漸層更快。慢衰減會讓畫面變成一片棕霧，
  而不是「一盞燈照亮周圍一圈」。
- 因此以下東西都**不可以**發光：按鈕、卡片、標籤、狀態提示、導覽列。
  它們一旦發光，就成了畫面上的第二個光源。

### 按鈕為什麼沒有外框
在只有一盞光的畫面裡，一個描邊的矩形會立刻變成第二個發亮的物件，
把注意力從香火／籤紙上拉走。所以按鈕只有：拉開的字距 + 一條細線。
可點擊的訊號來自字距、字色與底線，不是來自容器。

---

## 規則二：儀式置中對稱，瀏覽／管理靠左不對稱

| | 構圖 | 元件 | 屬於這一類的畫面 |
|---|---|---|---|
| 儀式 | 置中、左右對稱 | `CeremonyScreen` | 線上拜拜、抽籤、點燈 |
| 瀏覽／管理 | 靠左、右側留空 | `PageScreen` | 首頁、探索、神明空間、寺廟、點燈紀錄、祭改、信仰時光軸 |

理由：宮廟建築是中軸線的——你站在中門，正對神像，左右對稱。儀式畫面複製這個身體經驗。
而看名冊、翻紀錄、填表格是「在側廂做事」，不是「在中軸線上敬拜」，
所以刻意不給它對稱的莊嚴感。

兩個刻意的判斷：
- **神明空間**屬於瀏覽（仍然靠左），但光源被拉近、調亮——已經站在殿前，只是還沒進入儀式。
- **祭改**即使名字是「儀式」，也歸在管理側：它實際上是預約表單，真正的儀式在廟裡發生，
  不在這個畫面裡。這份視覺上的克制本身就是產品立場（見 RED_TEAM_REPORT.md）。

---

## 規則三：紙的暖白色只出現在真的是紙的東西上

`<Paper>` 元件只准用在：
- **籤紙**（抽到的籤詩、從紀錄回看的籤）
- **祈願卡**（拜拜寫下的願望、點燈的名條）

不准用在：列表、卡片、對話框、說明區塊、AI 解讀。**它們都不是紙。**

這條規則的價值在於：當畫面上出現一張暖白色的東西時，
使用者立刻知道「這是我寫的／我抽到的那一張」，而不是又一個 UI 容器。
也因此，紙上的字是**墨色**的——那是全站唯一的深色文字。

註：AI 解讀刻意不做成紙。它是旁人的說明，不是神明給的那張籤，
這個材質差異同時也是內容安全設計的一部分（見 SECURITY.md）。

---

## 規則四：不使用任何裝飾性圖案

質感只能來自三件事：**字體、留白、光影。**

- 沒有圖示字型、沒有 emoji、沒有符號圖案。導覽列只有文字。
  （原本用 ⌂ ☯ ☼ 卐 ☰ 當導覽圖示，已全部移除：那些符號分屬不同宗教／文化體系，
  混用只是「看起來很東方」；其中 卐 更有實際宗教意義且極易被誤讀，不適合當導覽圖示。）
- 沒有漸層色塊、毛玻璃、發光邊框、粒子效果。
- 圓角極小（2–4px）。夜裡的東西是建築與紙，不是圓潤的 App 卡片。
- 層次靠留白尺度與字級對比，不靠底色深淺堆疊。

---

## 調色盤

只有五個角色，沒有第六個。沒有品牌色、沒有狀態色階。

| Token | 用途 |
|---|---|
| `void` | 夜。頁面的地，近黑但帶木頭／香灰的暖度，不是純黑也不是藍黑 |
| `ash` | 被微光照到的字。**永遠不是純白**，因為夜裡沒有純白 |
| `ember` | 香爐的火（暖橘紅）。只用在真的是光的地方 |
| `flame` | 燭火／燈火（暖金）。同樣只用在光源本身 |
| `paper` | 紙。只准出現在真的是紙的東西上 |

**這個產品只有一種光線狀態：夜。** 不做淺色模式、不跟隨系統主題——
這不是「深色模式」，這是這個產品的樣子。

---

## 動態

唯一被允許無限循環的動畫是**火的呼吸**（`animate-breathe`，4.5 秒週期），
因為真實的火本來就不會靜止。幅度刻意很小，避免變成閃爍的裝飾效果。

其餘依 Motion System 原則（`src/lib/motionTokens.ts`）：
重要操作強烈回饋、一般操作微小回饋、閱讀時幾乎無動畫。
抽籤揭曉前的靜默（900ms）是整段節奏裡最重要的空白，不要為了「反應更快」把它拿掉。
